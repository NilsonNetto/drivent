import { AuthenticatedRequest } from "@/middlewares";
import { Response } from "express";

export async function functionName(req: AuthenticatedRequest, res: Response) {
  try {
    return;
  } catch (error) {
    return;
  }
}
